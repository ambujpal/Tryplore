import 'package:tryplore_shop/common/constant/app_colors.dart';
import 'package:flutter/cupertino.dart';

class AppShadows {
//#region Primary Shadow
  static const primaryShadow2dp = BoxShadow(
    color: AppColors.primary,
    blurRadius: 2,
    offset: Offset(0, 1),
  );

  static const primaryShadow4dp = BoxShadow(
    color: AppColors.primary,
    blurRadius: 4,
    offset: Offset(0, 2),
  );

  static const primaryShadow8dp = BoxShadow(
    color: AppColors.primary,
    blurRadius: 8,
    offset: Offset(0, 4),
  );

  static const primaryShadow16dp = BoxShadow(
    color: AppColors.primary,
    blurRadius: 16,
    offset: Offset(0, 8),
  );

  static const primaryShadow32dp = BoxShadow(
    color: AppColors.primary,
    blurRadius: 32,
    offset: Offset(0, 16),
  );
//# endregion

//#region Grey Shadow
  static const greyShadow2dp = BoxShadow(
    color: AppColors.grey,
    blurRadius: 2,
    blurStyle: BlurStyle.outer,
    offset: Offset(0, 1),
  );

  static const greyShadow4dp = BoxShadow(
    color: AppColors.grey,
    blurRadius: 4,
    offset: Offset(0, 2),
  );

  static const greyShadow8dp = BoxShadow(
    color: AppColors.grey,
    blurRadius: 8,
    offset: Offset(0, 4),
  );

  static const greyShadow16dp = BoxShadow(
    color: AppColors.grey,
    blurRadius: 16,
    offset: Offset(0, 8),
  );

  static const greyShadow32dp = BoxShadow(
    color: AppColors.grey,
    blurRadius: 32,
    offset: Offset(0, 16),
  );

//#endregion

//#region Success Shadow
  static const successShadow2dp = BoxShadow(
    color: AppColors.success,
    blurRadius: 2,
    offset: Offset(0, 1),
  );

  static const successShadow4dp = BoxShadow(
    color: AppColors.success,
    blurRadius: 4,
    offset: Offset(0, 2),
  );

  static const successShadow8dp = BoxShadow(
    color: AppColors.success,
    blurRadius: 8,
    offset: Offset(0, 4),
  );

  static const successShadow16dp = BoxShadow(
    color: AppColors.success,
    blurRadius: 16,
    offset: Offset(0, 8),
  );

  static const successShadow32dp = BoxShadow(
    color: AppColors.success,
    blurRadius: 32,
    offset: Offset(0, 16),
  );

//#endregion

//#region Error Shadow
  static const errorShadow2dp = BoxShadow(
    color: AppColors.error,
    blurRadius: 2,
    offset: Offset(0, 1),
  );

  static const errorShadow4dp = BoxShadow(
    color: AppColors.error,
    blurRadius: 4,
    offset: Offset(0, 2),
  );

  static const errorShadow8dp = BoxShadow(
    color: AppColors.error,
    blurRadius: 8,
    offset: Offset(0, 4),
  );

  static const errorShadow16dp = BoxShadow(
    color: AppColors.error,
    blurRadius: 16,
    offset: Offset(0, 8),
  );

  static const errorShadow32dp = BoxShadow(
    color: AppColors.error,
    blurRadius: 32,
    offset: Offset(0, 16),
  );

//#endregion

//#region Warning Shadow
  static const warningShadow2dp = BoxShadow(
    color: AppColors.warning,
    blurRadius: 2,
    offset: Offset(0, 1),
  );

  static const warningShadow4dp = BoxShadow(
    color: AppColors.warning,
    blurRadius: 4,
    offset: Offset(0, 2),
  );

  static const warningShadow8dp = BoxShadow(
    color: AppColors.warning,
    blurRadius: 8,
    offset: Offset(0, 4),
  );

  static const warningShadow16dp = BoxShadow(
    color: AppColors.warning,
    blurRadius: 16,
    offset: Offset(0, 8),
  );

  static const warningShadow32dp = BoxShadow(
    color: AppColors.warning,
    blurRadius: 32,
    offset: Offset(0, 16),
  );

//#endregion

}
